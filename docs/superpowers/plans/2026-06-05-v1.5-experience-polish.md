# Ptu v1.5 Experience Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make v1.5 feel like one coherent desktop workbench: left-side capture controls, right-side embedded Douyin browsing/login/tutorial, clearer progress, sharper live-photo output, and no flashing command windows.

**Architecture:** Keep FastAPI + pywebview as the main app shell, but replace the fake second Douyin window with a Windows-only native WebView2 child control mounted inside the main pywebview WinForms form. The HTML right dock owns layout and instructions; the Python bridge owns the child WebView2 lifecycle, navigation, cookie sync, and new-window interception.

**Tech Stack:** Python 3.13, FastAPI, pywebview WinForms/WebView2 backend, WebView2 .NET interop via pythonnet, vanilla JS/CSS, FFmpeg.

---

### Task 1: Native Right-Dock Douyin WebView

**Files:**
- Create: `backend/app/desktop_douyin_panel.py`
- Modify: `backend/app/js_api.py`
- Modify: `backend/app/static/js/app.js`
- Modify: `backend/app/templates/index.html`
- Test: `tests/test_douyin_browser_panel.py`

- [ ] Replace `webview.create_window()` with a `NativeDouyinPanel` object that creates a `Microsoft.Web.WebView2.WinForms.WebView2` child control on the main pywebview WinForms form.
- [ ] Mount and resize it from the browser dock host rectangle sent by JS.
- [ ] Intercept `NewWindowRequested` and load the requested URI in the same child WebView2.
- [ ] Keep the Douyin whitelist in Python; block non-Douyin navigation by falling back to `https://www.douyin.com/`.
- [ ] Remove the right-side copy-current-link button and expose a manual copy tutorial.

### Task 2: Right-Dock Login Flow

**Files:**
- Modify: `backend/app/templates/index.html`
- Modify: `backend/app/static/js/app.js`
- Modify: `backend/app/static/css/app.css`
- Test: `tests/test_login_status_feedback.py`

- [ ] Move QR login UI into the right dock.
- [ ] Clicking the login status opens the right-dock login panel instead of a centered modal.
- [ ] On successful login, hide the QR panel, sync cookies into the native WebView2 child, and navigate the right dock to Douyin home.

### Task 3: Rendering And Filesystem UX

**Files:**
- Modify: `backend/app/services/media_processor.py`
- Modify: `backend/app/services/downloader.py`
- Modify: `backend/app/services/live_photo.py`
- Modify: `backend/app/main.py`
- Modify: `backend/app/static/js/app.js`
- Test: `tests/test_media_processor_douyin_clean.py`
- Test: `tests/test_downloader_text_content.py`

- [ ] Raise generated video quality for live photos and slideshows: avoid needless upscaling blur, use higher-quality scaler, lower CRF, and preserve live-photo video streams where possible.
- [ ] Hide subprocess windows for all FFmpeg/ffprobe/live-photo commands.
- [ ] Make "打开文件夹" open only the folder, not auto-select or play the generated video.
- [ ] Keep generated video links explicit: users choose "打开视频" only when they want playback.

### Task 4: Progress And Theme Polish

**Files:**
- Modify: `backend/app/templates/index.html`
- Modify: `backend/app/static/css/app.css`
- Modify: `backend/app/static/js/app.js`
- Test: `tests/test_ui_render_copy.py`

- [ ] Move single-task progress below the action buttons/result controls so it is visible during download/render.
- [ ] Apply dark mode to the browser dock, native host placeholder, QR panel, tutorial, and diagnostic result.
- [ ] Clear stale preview state before every preview navigation so old videos cannot appear for a new link.

### Task 5: Verification And Packaging

**Files:**
- Modify: `scripts/release_check.py`
- Modify: `tests/test_release_check.py`
- Modify: `README.md`
- Modify: `CLAUDE.md`
- Modify: `README.md`, `CLAUDE.md`, or current release audit docs
- Modify: `docs/release_checklist.md`

- [ ] Add release checks that reject `webview.create_window()` for the Douyin panel and reject `copyBrowserUrl`.
- [ ] Run targeted tests, full pytest, compileall, JS syntax check, release check, build, installed smoke check, and sensitive-file audit.
